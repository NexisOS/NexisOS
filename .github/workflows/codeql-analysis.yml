name: "CodeQL (Disabled)"

on:  # Keep a manual trigger just in case
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Code scanning is currently disabled. Enable this workflow to begin analysis."
